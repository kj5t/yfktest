ARRL Sweepstakes						# contest name
exc4-all-all							# multiplier 1
dx=0~cont=2~own=2~exc1=.=2					# score
-								# multiplier 2
4								# # of exchange fields
4								# length of exchange field 1
1								# length of exchange field 2
2								# length of exchange field 3
3								# length of exchange field 4
[0-9]								# regex for every single character in exchange field 1
[0-9]+								# regex for complete valid exchange field 1
[a-zA-Z]							# regex for every single character in exchange field 2
[A-Z]								# regex for complete valid exchange field 2
[0-9]								# regex for every single character in exchange field 3
[0-9]{2,2}							# regex for complete valid exchange field 3
[a-zA-Z]							# regex for every single character in exchange field 4
[A-Z]{2,3}							# regex for complete valid exchange field 4
QSO: %9g %s %s %s %-10s %4s %s %-2s %s %-10s %4s %s %-2s %-3s	# cabrillo definition - how to format the info
freq mode date utc mycall nr exc1s exc2s exc3s call exc1 exc2 exc3 exc4	# cabrillo definition - what info is included
ARRL-SS-!MODE!							# CBR (cabrillo) name - "title" for cabrillo file
exc1s								# which fixed exchange is selected (variable assignment)
Prec./Chk./Sec.		 (e.g. Q/71/NM)				# what fixed exchange info is asked for (prompted)
CQ TEST MYCALL MYCALL TEST					# F1 Key - What will be sent in CW mode
NR EXC1S MYCALL EXC2S EXC3S					# F2		"
TU MYCALL							# F3		"
MYCALL								# F4		"
HISCALL								# F5		"
NR?								# F6		"
?								# F7		"
